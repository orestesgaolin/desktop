// ignore_for_file: file_names
import '../slide_layouts.dart';

class Slide16 extends TitleOnlySlideLayout {
  Slide16({super.key})
    : super(
        route: '/what-if-the-editor-were-flutter',
        title: 'What if the design tool were Flutter too?',
        label: 'GPU + editor',
      );
}
