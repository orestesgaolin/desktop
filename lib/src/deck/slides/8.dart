// ignore_for_file: file_names
import '../slide_layouts.dart';

class Slide8 extends TitleOnlySlideLayout {
  Slide8({super.key})
    : super(
        route: '/one-widget-tree',
        title: 'One widget tree can now span every window.',
        label: 'Windowing',
      );
}
