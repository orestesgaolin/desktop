import '../slide_layouts.dart';

class DesktopRawPowerDraft extends TitleOnlySlideLayout {
  DesktopRawPowerDraft({super.key})
    : super(
        route: '/desktop-raw-power',
        title: 'Desktop is where Flutter’s raw power shows.',
        label: 'GPU + editor',
      );
}
// ignore_for_file: file_names
